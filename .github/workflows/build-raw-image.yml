name: Build Raw Image

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Raw Disk Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Delete unnecessary tools to free up disk space
        run: sudo rm -rf /opt/hostedtoolcache

      - name: Build Bootc Container Image locally
        run: sudo podman build -t localhost/rhs-bootstrapper:latest -f Containerfile .

      - name: Build Raw Disk Image using bootc-image-builder
        run: |
          sudo mkdir -p ./output
          echo -e '[[customizations.filesystem]]\nmountpoint = "/"\nminsize = "3 GiB"' > ./config.toml
          sudo podman run \
            --rm \
            -it \
            --privileged \
            --pull=newer \
            --security-opt label=type:unconfined_t \
            -v ./config.toml:/config.toml \
            -v ./output:/output \
            -v /var/lib/containers/storage:/var/lib/containers/storage \
            quay.io/centos-bootc/bootc-image-builder:latest \
            --config /config.toml \
            build \
            --type raw \
            --rootfs xfs \
            localhost/rhs-bootstrapper:latest
            
      - name: Compress output file
        run: sudo gzip output/image/disk.raw

      - name: Rename output file
        run: sudo mv output/image/disk.raw.gz output/image/rhs-bootstrapper.raw.gz

      - name: Determine Release Tag
        id: release_tag
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "TAG_NAME=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
            echo "RELEASE_NAME=Release ${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          else
            echo "TAG_NAME=latest" >> $GITHUB_ENV
            echo "RELEASE_NAME=Latest Nightly Build" >> $GITHUB_ENV
          fi

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: ${{ env.RELEASE_NAME }}
          files: output/image/rhs-bootstrapper.raw.gz
          prerelease: ${{ env.TAG_NAME == 'latest' }}
          make_latest: ${{ env.TAG_NAME != 'latest' }}
